chromanode:
  network: regtest # livenet | testnet | regtest

logger:
  level: verbose # verbose, info, warning, error
  filename: cc-scanner.log

postgresql:
  url: postgres://chromaway:masonit@localhost:17520/dual
  poolSize: 10
